<h1> CD Organizer </h1>

<h3> Keep track of your CD collection here </h3>

<form action="/collection" method="post">
  <label for="artist_name">Artist's name:</label>
  <input id="artist_name" name="artist_name" type="text">

  <label for="album_name">Album name:</label>
  <input id="album_name" name="album_name" type="text">
  <button type="submit">Add CD</button>
</form>
<br>
<p> <% if @all_cdes.length() == 0 %>
  Your CD collection is empty.
  <% elsif @artist_add != nil %>
  <%=@album_add%> by <%=@artist_add %> was added to the library at position <%=@id%></p>
  <% end %>
</p>
<form action="/search" method="post">
  <label for="search_artist">Search by Artist name</label>
  <input id="search_artist" name="search_artist" type="text">
  <label for="search_album">Search by Album name</label>
  <input id="search_album" name="search_album" type="text">
  <label for="search_id">Search by location in library</label>
  <input id="search_id" name="search_id" type="text">
  <button type="submit">Search</button>
</form>

<% if @search_id != 0 && @search_id != nil %>
  <% if @search_id.<=(Organizer.counter()) %>
    Your album id: <%=@search_id%> holds this information
    <ul>
      <li>The artist name is <%= @result_id.artist_name() %></li>
      <li>The album is <%= @result_id.cd_name() %></li>
    </ul>
  <% else %>
    No luck buddy!
  <% end %>
<% else %>
  <% if @search_artist != "" && @search_album == "" %>
      <p> <%= @search_artist %> albums:  </p>
      <% @result_artist.each() do |cd_object| %>
        <ul>
          <li> <%= cd_object.cd_name() %> at location: <%= cd_object.id() %></li>
        </ul>
      <% end %>
  <% elsif @search_artist == "" && @search_album != "" %>
    <p>You have an album with name "<%= @search_album %>" by these artists:  </p>
      <% @result_album.each do |cd_object| %>
        <ul>
          <li> <%= cd_object.artist_name() %> at location: <%= cd_object.id() %></li>
        </ul>
      <% end %>
  <% elsif @search_artist == "" && @search_album == "" %>
        <% @result_artist.each() do |cd_object| %>
          <% if cd_object.cd_name() == @search_album %>
            <p> You have: <%= cd_object.artist_name() %>, cd_object.cd_name() %> at location: <%= cd_object.id() %> </p>
          <% end %>
        <% end %>
    <% end %>
  <% end %>
